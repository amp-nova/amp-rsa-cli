{
    "appId": "{{algolia.appId}}",
    "apiKey": "{{algolia.apiKey}}",
    "indexes": {
    {{#each algolia.indexes}}
        "{{@key}}": {
            "prod": "{{this.prod}}",
            "staging": "{{this.staging}}"
        }{{#unless @last}},{{/unless}}
    {{/each}}
    }
}